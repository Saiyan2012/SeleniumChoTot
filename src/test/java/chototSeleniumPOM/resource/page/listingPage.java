package chototSeleniumPOM.resource.page;
import org.openqa.selenium.WebDriver;

public class listingPage {
    WebDriver driver;

    public listingPage(WebDriver driver) {
        this.driver = driver;
    }

    public void checkLabelProtectedIsDisplay () {

    }
}
